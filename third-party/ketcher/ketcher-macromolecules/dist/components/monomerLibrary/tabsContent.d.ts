import { IRnaPreset } from './RnaBuilder/types';
import { TabsData } from 'components/shared/Tabs';
export declare const tabsContent: (duplicatePreset: (preset?: IRnaPreset) => void, editPreset: (preset: IRnaPreset) => void) => TabsData;
