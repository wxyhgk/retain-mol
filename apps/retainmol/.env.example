# Optional FastAPI + xTB backend override.
# Leave unset during local/LAN development: the app uses the current page
# hostname with port 8000, so 0.0.0.0/LAN-hosted Vite pages reach the same host.
# VITE_RETAINMOL_BACKEND_URL=http://compute-host.example:8000
