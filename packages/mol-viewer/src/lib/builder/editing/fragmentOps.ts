/**
 * fragmentOps — compatibility barrel for fragment editing operations.
 *
 * Implementation lives in ./fragment so placement, attach and ring-fuse logic
 * can evolve independently without growing this historical entry file again.
 */

export * from './fragment'
